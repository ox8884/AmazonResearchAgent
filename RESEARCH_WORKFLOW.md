# Forge Kitchen — 웹·API 혼합 리서치 설계

작성: 2026-09-04 America/Chicago. 상태: **구체 설계 검토본, 미구현·미배포**.
사용자는 예산·실원가 중심 방향 전환을 승인했다. 아래 저장 계약·자동 호출 정책은 구현 전 검토 대상이다.

## 1. 목적과 범위

목적은 분석 건수나 GO 개수 증가가 아니라, 총 출시 현금 $3,000 이내에서 검증 가능한 수익성과 실행 가능성을 가진 Forge Kitchen 제품을 찾는 것이다.
‘견적 요청 가치 있음’과 ‘발주 승인’을 구분한다. 한 차례에 합격 제품이 없을 수 있으며 점수나 조건을 낮춰 GO를 만들지 않는다.

- 유지: Cloudflare 웹, Oracle systemd worker, Supabase 큐/예산/인증, 기존 사용자 데이터와 공급처 대화.
- 변경 대상: 사업 근거 계약, 결정 단계와 다음 행동, 웹 수집 인계, API 작업 선택.
- 제외: 자동 발주·결제·업체 메시지 전송, 브라우저 쿠키의 Oracle 이관, 구독 OAuth, 새 브라우저 서비스, 전체 UI 리디자인, 신규 데이터 제공업체.
- $3,000은 판매 후 공제되는 수수료 전액을 초기에 더하는 방식이 아니라, 실제 지급 시점별 출시 현금 한도다. 단위 손익과 출시 현금을 분리한다.

## 2. 선택한 방식

| 방식 | 장점 | 문제 |
| --- | --- | --- |
| 웹+API 혼합형 — 선택 | 웹에서 넓게 발굴, API로 필요한 근거만 반복 검증 | 웹 세션이 끊기면 사람이 재로그인해야 함 |
| API 우선 전량 탐색 | 서버 단독 처리 가능 | 부적합 후보·중복 조회에 호출을 쓰기 쉬움 |
| 웹 전용 | API 호출 억제 | 재현성·예약 실행·정형 데이터 연결이 약함 |

웹 구독을 무제한 무료 API로 간주하지 않는다. CSV 내보내기와 API 사용 권한은 실제 계정에서 따로 확인한다. 로그인되어 있는 Codex 인앱 브라우저에서 웹 조사하고, Oracle에는 공식 API 작업만 상시 배치한다. PC가 꺼져도 저장된 후보의 API 작업은 진행되지만 새로운 웹 조사까지 자동으로 계속된다고 표시하지 않는다.

## 3. 단계와 다음 행동

| 단계 | 필요한 근거 | 다음 행동 | API 정책 |
| --- | --- | --- | --- |
| 신규 후보 | 웹/CSV 출처, US marketplace, 제품군 | 중복·카테고리·취급 위험 확인 | 자동 심층 호출 없음 |
| 기초 확인 | 관련 제품/판매가, 원가 가능성 자료 | 공개 공급가·MOQ·포장 범위 조사 | 웹으로 핵심 시장 근거를 얻지 못하면 제한된 1차 확인 요청 가능 |
| 시장 검증 | 정확한 사양 비교군, 원가·현금 가능성 | 부족하거나 오래된 시장 근거 수집 | 예산 내 필요한 endpoint만 |
| 견적 대상 | 시장 검증 통과, 잠정 손익/현금 가능성, 공급가 headroom | 동일 사양 견적 초안 작성, 사용자 승인 후 연락 | 반복 심층 호출 중단 |
| 견적/샘플 대기 | 요청한 사양·수량·배송 범위 및 응답 근거 | 회신 정리·조건 비교·샘플 확인 | 결정에 필요한 만료 근거만 명시적 재검증 |
| 발주 검토 가능 | 실제 견적·포장·수수료 추정·샘플·필요 안전/권리 검토 | 사용자 발주 판단 | 자동 발주 금지 |
| 보류 / 탈락 | 보류는 누락 근거, 탈락은 확인된 부적합 사유 | 조건 변경 또는 새 근거가 있을 때만 재개 | 동일 사유 반복 호출 없음 |

견적 없음을 탈락으로 취급하지 않는다. 미확인 때문에 예산 통과로 간주하지도 않는다. 기초 단계에서 제한된 시장 조회를 허용해 ‘시장자료가 없어서 원가 상한을 못 정하고, 원가 상한이 없어서 시장조회도 못 하는’ 순환을 피한다.

기존 후보 state/작업 status는 실행 상태로 보존한다. 사업 단계는 typed evidence에서 파생하며 새로운 job status로 섞지 않는다. 기존 Strong/분석 점수만으로 사업 단계를 올리지 않는다.

## 4. Jungle Scout 웹 조사 절차

1. US / Forge Kitchen 적합 범위로 Opportunity Finder를 검색한다. 평균 가격·수요·경쟁·계절성 필터의 실제 적용값을 기록한다. Niche Score는 탐색 정렬용이며 합격 기준이 아니다.
2. 처음부터 성공을 보장하는 좁은 숫자 조합을 고정하지 않는다. 초기 운영 배치 상한 제안은 서로 다른 제품군 30개, 상세 비교 10개, API 심층 후보 3개다. 상한이지 채워야 할 목표가 아니다.
3. Top Products/상품 페이지에서 동일 용도·재질·크기·구성의 비교군을 확정한다. 부모/변형 중복을 제거하고 제외 ASIN과 사유를 보존한다. 낮은 리뷰 상품이 팔리는지, 매출이 소수 브랜드에 몰리는지 확인한다.
4. Opportunity Finder 원본 CSV를 기존 import로 넣는다. 원본·행 번호·source hash를 유지한다. 다른 화면의 CSV를 Opportunity Finder 형식이라고 속이거나 필수 누락 값을 만들어 넣지 않는다.
5. Top Products와 수동 웹 관찰은 별도 출처 근거로 기록한다. 기존 import가 이 형식을 지원한다고 가정하지 않는다. 최소 관찰 입력: URL, 관찰 시각, 데이터 기간, marketplace, ASIN/제품군, 값·단위, estimated/observed 구분, 필터·제외 사유.
6. 웹 로그인이 없거나 접근 제한이 있으면 ‘웹 확인 대기’. CAPTCHA/제한 우회, 세션키 복사, 비공식 private endpoint 호출은 하지 않는다.

## 5. 공식 API를 쓰는 순서

| 정보 공백 | 기존 adapter | 실행 조건 / 중단 조건 |
| --- | --- | --- |
| 실제 비교 ASIN군·상품 속성 부족 | Product Database | 최소 필터로 조회. 관련성이 낮으면 검색어/사양을 먼저 고치고 다음 페이지를 무조건 돌지 않음 |
| 검색량 근거 부족 | Keywords | 정확한 검색어·기간 기록. CSV와 같은 기간/의미이고 충분히 최신이면 중복 조회 안 함 |
| 계절성·일회성 급증 불확실 | Historical Search Volume | 시장 검증 후보에 한해 지원 기간 조회. 계정의 이력 범위를 넘는 완전성을 주장하지 않음 |
| 경쟁 상품 판매 지속성 불확실 | Sales Estimates | 대표·저리뷰·시장 상위 등 이유 있는 ASIN부터 순차 조회. 날짜별 합계를 기간 확인 없이 월 판매량으로 부르지 않음 |
| 브랜드 장악력 불확실 | Share of Voice | 앞선 제품군 분석으로 답이 안 나는 경우만. 계약 미지원/권한 부족은 보류이며 0으로 대체하지 않음 |

웹과 API는 동일 제공업체 데이터일 수 있으므로 두 번 보였다고 독립된 두 증거로 세지 않는다. JS 판매량은 추정값으로 유지하며 우리 신규 상품의 예상 판매량으로 복사하지 않는다.

작업 선택 우선순위는 명시적 결정 재검증 → 실행 가능한 시장 검증 후보의 필수 공백 → 제한된 신규 후보 확인이다. 같은 우선순위 안에서 막힌 결정과 근거 만료를 기준으로 정렬한다. 임의의 ‘성공 확률’ 숫자는 생성하지 않는다.

- 기존 일/월 제공업체 한도와 DB atomic authorization을 유지한다. 설정된 한도는 상한이지 소비 목표가 아니다.
- API 논리 작업 수와 실제 wire request/page 수를 분리 표시한다. batch 하나를 1 call이라고 단정하지 않는다.
- 429/5xx는 현재 승인·재시도 정책을 따르며 예산을 우회하는 숨은 재시도를 추가하지 않는다.
- cache key에는 endpoint, marketplace, 정규화 query/ASIN, 기간, 필터, 버전을 포함한다. 수집 시각과 원천 데이터 기간을 분리한다.
- MOQ/견적 회신만 기다리는 후보를 매일 시세 재조회하지 않는다. 공급가/사양/시장 근거 변경 시 필요한 단계만 재개한다.
- 계약 권한과 계정 잔여량을 현재 확인하기 전에는 새 API 할당량을 자동 증가시키지 않는다.

## 6. 예산·손익 근거 계약

기존 candidate_evidence에 명시적 버전의 사업 근거를 추가하는 방식을 우선 검토한다. 저장 형식·입력 API 추가는 이 설계 승인 후 additive 변경으로 수행한다. 기존 economics_verified를 새 계약에 자동 승격하지 않는다.

필수 항목:

- 출시 계획: budget USD 3000, 초기 수량, 별도 선지급 출시비·광고 현금·예비비. 예비비 $1000은 예시일 뿐 자동 확정하지 않는다. 미입력은 현금 검증 미완료다.
- 사양: 재질·크기·구성·브랜딩/포장, packed dimensions/weight, marketplace.
- 공급 근거: supplier, URL/견적 참조, 수량별 단가·MOQ·통화·날짜·유효기간·납기, incoterm, 배송 도착지와 포함/제외 비용. 서로 다른 견적의 최저 단가와 MOQ를 임의 조합하지 않는다.
- 판매/비용 근거: 비교 사양에 맞춘 보수적 판매가, referral/FBA 및 관련 비용의 값·시점·출처, 광고/반품 등 명시적 시나리오. 추정 수수료는 확정 청구액으로 표현하지 않는다.
- 각 필드: measured/estimate/quote/unknown 및 출처. evidence 존재나 supplier_verified 라벨 하나만으로 수익성 확인 처리하지 않는다.

단위 기여이익 = 판매가 - 도착원가 - 해당 판매에 귀속되는 Amazon 비용 - 광고 - 반품/손실 예상액 - 기타 변동비.
출시 현금 = 초도 재고의 도착원가 + 별도 선지급 비용 + 초기 광고 현금 + 예비비. 이미 도착원가에 포함된 물류/포장 비용을 다시 더하지 않는다.
판매 수량·재고 회전·대금 회수 시점은 별도 가정이다. 시장 전체 판매량으로 우리 현금 회수 기간을 계산하지 않는다.
광고 후 목표 마진 기준 최대 도착원가 = 판매가 × (1 - 목표 광고후 마진율 - 광고율) - 기타 단위 비용.
광고 전·후 목표가 모두 있으면 각각의 원가 상한 중 작은 값을 적용한다. 기존 helper의 사용되지 않는 targetPostAdMarginPct와 고정 마진 점수는 이 계약에 맞춰 교정한다.

가격 하락·도착원가 상승·광고 증가 시나리오는 가정값을 화면에 보이게 한다. 불확실한 항목을 0으로 넣어 통과시키지 않는다. 마진/ROI 합격 정책은 기존 사용자 기준을 읽어 명시적 버전으로 고정하며, 승인 없이 낮추지 않는다.

## 7. 화면과 일일 보고

상단: 총 출시 한도 / 후보별 필요 현금 / 남은 예산이 아니라 계획별 여유(동시에 모든 후보를 발주하는 것처럼 합산하지 않음).
후보마다 제품군, 사업 단계, 확인된 근거, 남은 장애물, 다음 행동 하나를 보여준다.
버튼은 ‘시장 검증’, ‘원가 자료 입력’, ‘견적 초안’, ‘회신 기록’, ‘발주 검토’로 기능을 구분한다. 발주 검토는 주문 전송 버튼이 아니다.
일일 요약은 신규 분석 개수보다 단계가 진전된 후보·탈락 사유·다음 행동을 우선한다. 변화 없으면 ‘변화 없음’, 호출하지 않았으면 ‘0회’로 표시한다.

## 8. 최소 구현 순서와 검증

1. 사업 근거 계약·예산/원가 계산·Strong 권한 정정. 기존 economics/scoring/enrichment 테스트에 누락·0/음수·단가별 수량·중복 비용·$3000 초과 사례를 추가한다.
2. 기존 후보 상세와 import에 근거 입력/다음 행동 연결. CSV 재업로드·동의어/변형·시간 갱신이 사용자 기록을 손상시키지 않는지 검증한다.
3. daily planner를 사업 단계+필수 공백 기반으로 연결. 기존 queue/API authorization 경계에서 탈락/견적대기 추가 호출 0, fresh cache hit 0 wire call, 중복 실행 1회만 검증한다.
4. 격리된 fixture DB로 웹 import → 예산 확인 → API wire fixture → 견적대기 전환을 E2E 검증. 로그인/암호화/운영 키는 시험용으로 복제하지 않는다.
5. 변경된 UI를 375/768/1280 viewport에서 실제 브라우저로 확인하고 before/after·검증 로그를 분리 보존한다.
6. 코드 검토 후 Cloudflare+Oracle 동일 product source 반영. 운영 데이터는 읽기 전용 점검 우선, 별도 승인한 실제 후보 한 건의 제한된 API 검증으로 wire count/저장 결과 확인. 이전 release를 보존한다.

최소 변경 후보: packages/shared의 사업 근거 schema, packages/research-engine의 economics/scoring/planner, apps/worker의 daily/enrichment, apps/web의 후보 근거 입력·표시와 관련 route/tests. 새 dependency/브라우저 인프라는 추가하지 않는다. 기존 raw import/queue/secret-store를 재작성하지 않는다.

## 9. 확인한 공식 자료와 남은 확인

- [Opportunity Finder](https://support.junglescout.com/hc/en-us/articles/360037689513-Opportunity-Finder): 키워드 기반 탐색과 평균 수요·가격·경쟁·계절성 필터. 초기 발굴 근거이지 발주 승인 근거가 아니다.
- [API documentation](https://developer.junglescout.com/api): 공식 REST, marketplace/인증/버전, pagination 및 오류 계약. 기존 adapter 재사용 전 해당 endpoint의 현재 응답 shape를 검증한다.
- [API product](https://www.junglescout.com/products/jungle-scout-api/): 판매량 추정·검색 이력 등 사용 범위 참고.
- [Membership plans](https://support.junglescout.com/hc/en-us/articles/26264588139799-Information-about-our-Membership-Plans): CSV/API는 플랜별 제한이 있을 수 있다.
- [Amazon costs](https://sell.amazon.com/blog/how-to-use-fba-calculator?mons_sel_locale=en_US): 수수료 계산과 별도 비용 입력 참고.

이번 설계에서는 실제 JS 계정의 CSV 권한·잔여 API량·이력 범위를 재확인하지 않았다. 새 유료 조회, 업체 연락, DB migration, 배포도 수행하지 않았다. 구현 시작 전 이 문서의 저장·자동 호출 범위 승인과 실제 계정 권한 확인이 필요하다.
